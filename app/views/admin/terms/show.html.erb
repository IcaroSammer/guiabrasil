<br>
<section class='fields-wrapper two-columns'>
    <header>
      	<h2> <%= "Termo #{@term.name} " %> 
      		<%= edit_resource edit_admin_term_path(@term) %>
      	</h2>
    </header>
</section>
  <section class='fields-wrapper two-columns'>    
      <article>
            <br>
            <b><%= Term.human_attribute_name(:active) %>:&nbsp;</b>
            <%= @term.active %><br>
            <b><%= Term.human_attribute_name(:name) %>:&nbsp;</b>
            <%= @term.name %><br>
            <b><%= Term.human_attribute_name(:description) %>:&nbsp;</b>
            <%= @term.name %><br>
            <b><%= Term.human_attribute_name(:type_term) %>:&nbsp;</b>
            <%= @term.active %><br>
            <b><%= Term.human_attribute_name(:publication_date) %>:&nbsp;</b>
            <%= @term.name %><br>
            <b><%= Term.human_attribute_name(:inactivated_date) %>:&nbsp;</b>
            <%= @term.name %><br>
            <b><%= Term.human_attribute_name(:publicated_by) %>:&nbsp;</b>
            <%= @term.name %><br>
            <b><%= Term.human_attribute_name(:link_term) %>:&nbsp;</b>
            <%= @term.name %><br>
            
            
      </article>
  </section>

  <footer>
    <%= cancel_action(path: admin_terms_path) %>
    
  
  </footer>
