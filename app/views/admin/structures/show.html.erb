<br>
<section class='fields-wrapper two-columns'>
    <header>
      	<h2> <%= "Categoria conforto #{@structure.name} " %> 
      		<%= edit_resource edit_admin_structure_path(@structure) %>
      	</h2>
    </header>
</section>
  <section class='fields-wrapper two-columns'>    
      <article>
            <br>
            <b><%= Structure.human_attribute_name(:active) %>:&nbsp;</b>
            <%= @structure.active %><br>
            <b><%= Structure.human_attribute_name(:name) %>:&nbsp;</b>
            <%= @structure.name %><br>
            
      </article>
  </section>

  <footer>
    <%= cancel_action(path: admin_structures_path) %>
    
  
  </footer>
