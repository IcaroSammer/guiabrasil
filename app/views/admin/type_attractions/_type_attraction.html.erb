<td>
	<%= type_attraction.name %>
</td>
<td>
    <% if type_attraction.active %>
      <i class="icon-thumbs-up" style="color: green;"></i>
    <% else %>
      <i class="icon-thumbs-down" style="color: red;"></i>
    <% end %>
  </td>
  <td>
    <%= show_resource_with_icon admin_type_attraction_path(type_attraction) %>
    <%= edit_resource edit_admin_type_attraction_path(type_attraction) %>
    <%# remove_resource(admin_type_attraction_path(type_attraction), {})%>
    <% if type_attraction.active == true %>
  <%= link_to raw('<i class="icon-cancel" style = "color:red"></i>'), admin_type_attraction_path(type_attraction),
              method: :delete,
              data: { confirm: 'Are you sure?' } %></td>
  <% else %>
     
  <%= link_to raw('<i class="icon-check"style = "color:green"></i>'), admin_type_attraction_path(type_attraction),
              method: :delete,
              data: { confirm: 'Are you sure?' } %></td>
   <%end%>
  </td>
</tr>