<br>
<section class='fields-wrapper two-columns'>
    <header>
      	<h2> <%= "Prato #{@publication.name} " %> 
      		<%= edit_resource edit_user_main_course_path(@publication) %>
      	</h2>
    </header>
</section>
  <section class='fields-wrapper two-columns'>    
      <article>
            <br>
            <b><%= Publication.human_attribute_name(:active) %>:&nbsp;</b>
            <%= @publication.active %><br>
            <b><%= Publication.human_attribute_name(:name) %>:&nbsp;</b>
            <%= @publication.name %><br>
            <b><%= Publication.human_attribute_name(:description) %>:&nbsp;</b>
            <%= @publication.description %><br>
            <b><%= Publication.human_attribute_name(:tag) %>:&nbsp;</b>
            <%= @publication.tag %><br>
            
      </article>
  </section>

  <footer>
    <%= cancel_action(path: user_publications_path) %>
    
  </footer>
